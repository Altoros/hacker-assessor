<%= form_for(@hacker) do |f| %>
  <% if @hacker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hacker.errors.count, "error") %> prohibited this hacker from being saved:</h2>

      <ul>
      <% @hacker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, :placeholder => :name %>
  </div>
  <div class="field">
    <%= f.text_field :email, :placeholder => :email %>
  </div>
  <div class="field">
    <%= f.password_field :password, :placeholder => :password %>
  </div>
  <div class="field">
    <%= f.password_field :password_confirmation, :placeholder => :password_confirmation %>
  </div>
  <div class="field">
    <%= f.label :career_id %>
    <%= f.select :career_id, Career.all.collect { |p| [ p.name, p.id  ] } %>
  </div>
  <div class="field">
    <%= f.label :seniority %>
    <%= f.seniority_select %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
